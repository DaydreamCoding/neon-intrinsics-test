set(BENCHMARK_ROOT_PATH "${CMAKE_CURRENT_LIST_DIR}")

if(MSVC OR WIN32)

    # using Visual Studio C++
    if (CMAKE_SIZEOF_VOID_P MATCHES 8)
        set(FaceRetouchEngine_WIN_ARCH "x64")
    else()
        set(FaceRetouchEngine_WIN_ARCH "x86")
    endif()
    
    include("${BENCHMARK_ROOT_PATH}/Win/${WIN_ARCH}/lib/cmake/benchmark/benchmarkConfig.cmake")
elseif (ANDROID)
    set(ANDROID_DIR "android")
    if(${ASan_IS_ENABLE})
        set(ANDROID_DIR "android-asan")
    elseif(${HWASan_IS_ENABLE})
        set(ANDROID_DIR "android-hwasan")
    endif()
    include("${BENCHMARK_ROOT_PATH}/${ANDROID_DIR}/${ANDROID_ABI}/usr/lib/cmake/benchmark/benchmarkConfig.cmake")
elseif (APPLE AND IOS)
    include("${BENCHMARK_ROOT_PATH}/iOS/usr/lib/cmake/benchmark/benchmarkConfig.cmake")
elseif (APPLE)
    set(macOS_DIR "macOS")
    if(${ASan_IS_ENABLE})
        set(macOS_DIR "macOS-asan")
    endif()
    include("${BENCHMARK_ROOT_PATH}/${macOS_DIR}/usr/lib/cmake/benchmark/benchmarkConfig.cmake")
elseif (UNIX)
    include("${BENCHMARK_ROOT_PATH}/ubuntu/usr/lib/cmake/benchmark/benchmarkConfig.cmake")
else ()
    message(FATAL_ERROR "GTest not support this platform")
endif ()